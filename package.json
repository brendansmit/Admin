{
  "name": "inkheron-admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "serve:start": "node serve/server.js",
    "serve:test": "node --test serve/test/*.test.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
